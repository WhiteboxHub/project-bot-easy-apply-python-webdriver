TODO: Implementing rotating residential proxies

Browser and session handling
	1.	Integrate undetected chromedriver
Replace standard Selenium driver with undetected chromedriver for browser initialization.
	2.	Load existing Chrome profile
Configure undetected chromedriver to load a specified user data directory and profile so cookies, sessions, and preferences are reused.
	3.	Profile safety checks
Add validation to ensure the profile is not already in use and abort execution if Chrome is running.

⸻

Architecture and modularization
	4.	Modularize bot architecture
Split the codebase into clearly defined components for browser control, job discovery, application workflow, and persistence.
	5.	Define module boundaries and contracts
Ensure each module has a single responsibility and clean interfaces, no cross module DOM access.

⸻

Selector management
	6.	Centralize selector definitions
Create a single selector registry with semantic names instead of inline selectors.
	7.	Add selector fallbacks
Define primary and fallback selectors for critical elements such as job cards, apply button, continue, and submit.
	8.	Selector validation
Add startup or page level validation for required selectors with clear failure reporting.

⸻

Logging and observability
	9.	Standardize logging schema
Ensure all logs include job ID, step name, log level, and message.
	10.	Error logging enrichment
Include exception type in all error and warning logs.
	11.	Audit friendly logs
Ensure logs are structured and suitable for later parsing or storage.

⸻

Navigation hardening
	12.	Retry framework for navigation
Add capped retries for critical actions such as opening job cards and clicking apply or submit.
	13.	Controlled abort on retry exhaustion
Fail fast with explicit errors when retry limits are exceeded.

⸻

DOM stability handling
	14.	Explicit stale element detection
Detect stale element reference errors explicitly rather than catching generic exceptions.
	15.	Stale element recovery
Re locate elements cleanly and retry actions using fresh references.

⸻

Scroll and pagination control
	16.	Track scroll state
Track scroll height and visible job card counts across iterations.
	17.	Prevent infinite scrolling
Stop scrolling when no new job cards appear after multiple attempts.
	18.	Job deduplication during scroll
Track seen job IDs to avoid re processing the same listing.

⸻

Execution safeguards
	19.	Maximum applications per run
Enforce a hard cap on how many applications can be submitted in a single run.
	20.	Submission cooldown enforcement
Add a mandatory cooldown period between successful submissions.
	21.	Central execution guard
Centralize application limits so they cannot be bypassed accidentally.

⸻

Dry run capability
	22.	Implement dry run mode
Add a configuration flag that runs the full flow without submitting applications.
	23.	Dry run logging
Clearly log what actions would have been taken, especially submit actions.
	24.	Selector and flow validation via dry run
Use dry run mode to validate selectors and navigation safely.

⸻

Metrics and reporting
	25.	Application metrics tracking
Track counts for attempted, submitted, skipped, failed, and retried applications.
	26.	Per step metrics
Capture retry counts and failure reasons by step.
	27.	End of run summary
Emit a clear summary report at the end of each run with all metrics.

⸻

Network and fingerprint strategy
	28.	Rotating residential proxy support
Design integration points for rotating residential proxies, configurable per run or per session.
	29.	Variable browser fingerprinting
Integrate fingerprint randomization libraries such as selenium stealth at browser startup.
	30.	Natural human interaction patterns
Add mouse micro movements, varied scrolling speeds, and non uniform interaction timing beyond simple sleep delays.

⸻

Persistence layer
	31.	DuckDB integration
Replace CSV output with a local DuckDB database for application records.
	32.	Schema design for applications
Define tables for jobs, runs, applications, retries, and failures.
	33.	Migration path
Provide a one time migration from existing CSV data into DuckDB.

⸻

Security and configuration
	34.	Secure credential management
Move usernames and passwords out of config files into environment variables or a secret manager.
	35.	Add environment file support
Implement dotenv loading and provide a .env.example file with placeholders.
	36.	Secret validation at startup
Validate required secrets are present before execution begins.
